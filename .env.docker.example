# Development-only values. Copy to .env to override Compose defaults if needed.
MYSQL_ROOT_PASSWORD=change-me-for-non-local-use
JWT_SECRET=replace-with-a-base64-encoded-secret-of-at-least-32-bytes
VITE_API_BASE_URL=http://localhost:8080
